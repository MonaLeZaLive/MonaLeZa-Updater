name: Update Football Matches

on:
  # 🔴 تشغيل يدوي عند الحاجة
  workflow_dispatch:

  schedule:
    # 🕛 مرة واحدة يوميًا
    - cron: "5 0 * * *"

    # 🔴 تحديث اللايف
    - cron: "*/5 * * * *"

jobs:
  update-matches:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      # 🕛 سحبة بداية اليوم (أمس / اليوم / غد)
      - name: Update daily matches
        if: github.event.schedule == '5 0 * * *'
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node updateMatches.js

      # 🔴 تحديث اللايف فقط
      - name: Update live matches
        if: github.event.schedule == '*/5 * * * *'
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node livematches.js
